# Core dependencies
python-dotenv>=1.0.0
openai>=1.0.0
chromadb>=0.5.0
pydantic>=2.0.0
aiofiles>=23.0.0
typing-extensions>=4.0.0

# Optional: Parser dependencies
# magic-pdf  # MinerU parser (install separately if needed)
# docling    # Docling parser (install separately if needed)

# Optional: Extended format support
# Pillow>=10.0.0      # For extended image formats (install with: pip install raganything[image])
# ReportLab>=4.0.0     # For text processing (install with: pip install raganything[text])

# Optional: UI support
# streamlit>=1.28.0   # For web UI (install with: pip install raganything[ui])

# Development dependencies (install with: pip install raganything[dev])
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# isort>=5.12.0
# pylint>=3.0.0
# mypy>=1.0.0
# pre-commit>=3.0.0

